server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # This is the key fix — fallback to index.html for all routes
    location / {
        try_files $uri $uri/ /index.html;
    }
}